version: 0.2 # 임의의 버전 이름
phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - npm install
  build:
    commands:
      - npm run build
artifacts: # CodeBuild의 output artifacts를 지정합니다.
  files:
    - '**/*'
